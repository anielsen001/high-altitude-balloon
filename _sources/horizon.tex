\documentclass{standalone}

\usepackage{tikz}
\usetikzlibrary{positioning}
\usetikzlibrary{math}
\usetikzlibrary{angles, quotes}
%\usetikzlibrary{shapes.misc}
%\usetikzlibrary{shapes,arrows}

%\usepackage{scalefnt}
%\usepackage{fontawesome}
%\usepackage{amsmath,amssymb}

\begin{document}
\begin{tikzpicture}

\newcommand\radius{4.0}
\newcommand\height{1.0}

\coordinate (O) at (0,0) ;

\coordinate (R) at (\radius,0); 
\coordinate (h) at (\height+\radius,0);

\coordinate (I) at ( {\radius* (\radius/(\radius + \height)) }, {\radius*sin(acos(\radius/(\radius + \height)))}); 

\draw[fill=none] (0,0) circle (\radius);
\draw[fill=black] (0,0) circle (1 pt); 
\draw[](0,0)--(R) node [midway,below] {$R$}; 
\draw[](R)--(h) node [midway, below] {$h$}; 

\draw[](O)--(I) node [midway,left] {$R$}; 

% slant range to horizon
\draw[](I)--(h) node [midway,right] {$s$};

\path 
        (h)
        -- (O)
        -- (I)
pic[draw, 
    "$\theta$", 
    thick,
    <->, 
    angle eccentricity= 1.2,
    angle radius = 1cm] {angle = h--O--I};



\path 
    (h)
    -- (O)
    -- (I)
pic[draw, 
"$\ell$", 
thick,
---, 
midway,
left,
angle eccentricity= 1,
angle radius = \radius cm] {angle = h--O--I};


\end{tikzpicture}
\end{document}
